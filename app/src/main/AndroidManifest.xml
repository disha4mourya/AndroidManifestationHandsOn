<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidmanifestation">

    <!--permission required for internet access-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddTaskActivity" />
        <activity android:name=".server_calls.ServerCallOptions" />
        <activity android:name=".server_calls.other_ways.NoLibraryCallActivity" />
        <activity android:name=".server_calls.other_ways.LoopjCallActivity" />
        <activity android:name=".server_calls.other_ways.VolleyServerCall" />
        <activity android:name=".server_calls.other_ways.OkhttpCallActivity" />

        <!--needed for old http call-->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>


</manifest>